/**
 * Generated by Sideko (sideko.dev)
 **/

import { zodRequiredAny, zodTransform } from "make-api-request-js";
import * as z from "zod";

/**
 * AgentCreateBody
 */
export type AgentCreateBody = {
  /**
   * The name of the agent to run (e.g. "orchestrator", "querier", "invoker").
   */
  agent: string;
  /**
   * The ID of the chat session. When provided, messages are persisted to storage so they can be polled by the client.
   */
  chatId?: string | undefined;
  /**
   * Additional context to pass to the agent (e.g. current URL, page state).
   */
  context?: Record<string, any | undefined> | undefined;
  /**
   * Maximum number of tool-call iterations before the agent is forced to respond. Defaults to 15.
   */
  maxIterations?: number | undefined;
  /**
   * The user message / task for the agent to complete.
   */
  message: string;

  // Index types must also accommodate all defined properties if present (typescript limitation)
  // In reality all additional properties should only have type `any`
  // (there might be some duplication here)
  [additionalProperty: string]:
    | string
    | (string | undefined)
    | (Record<string, any | undefined> | undefined)
    | (number | undefined)
    | string
    | any
    | null
    | undefined;
};

/**
 * @internal
 * AgentCreateBody without any key transformation, this is what
 * we expect to come in as network data
 */
export type External$AgentCreateBody = {
  agent: string;
  chat_id?: string | undefined;
  context?: Record<string, any | undefined> | undefined;
  max_iterations?: number | undefined;
  message: string;

  [additionalProperty: string]:
    | string
    | (string | undefined)
    | (Record<string, any | undefined> | undefined)
    | (number | undefined)
    | string
    | External$AgentCreateBody
    | null
    | undefined;
};

/**
 * Takes network data, validates it, and transforms keys to match typescript object AgentCreateBody
 */
const SchemaIn$AgentCreateBody: z.ZodType<
  AgentCreateBody, // output type of this zod object
  z.ZodTypeDef,
  unknown
> = z
  .object({
    agent: z.string(),
    chat_id: z.string().optional(),
    context: z.record(z.string(), zodRequiredAny.optional()).optional(),
    max_iterations: z.number().optional(),
    message: z.string(),
  })
  .catchall(zodRequiredAny)
  .transform((obj) => {
    return zodTransform(obj, {
      agent: "agent",
      chat_id: "chatId",
      context: "context",
      max_iterations: "maxIterations",
      message: "message",
    });
  });

/**
 * @internal
 * Takes typescript data, validates it, and maps keys to match the expected external object External$AgentCreateBody
 */
const SchemaOut$AgentCreateBody: z.ZodType<
  External$AgentCreateBody, // output type of this zod object
  z.ZodTypeDef,
  AgentCreateBody // the object to be transformed
> = z
  .object({
    agent: z.string(),
    chatId: z.string().optional(),
    context: z.record(z.string(), zodRequiredAny.optional()).optional(),
    maxIterations: z.number().optional(),
    message: z.string(),
  })
  .catchall(zodRequiredAny)
  .transform((obj) => {
    return zodTransform(obj, {
      agent: "agent",
      chatId: "chat_id",
      context: "context",
      maxIterations: "max_iterations",
      message: "message",
    });
  });

export const Schemas$AgentCreateBody = {
  in: SchemaIn$AgentCreateBody,
  out: SchemaOut$AgentCreateBody,
};
