/**
 * Generated by Sideko (sideko.dev)
 **/

import { zodTransform } from "make-api-request-js";
import * as z from "zod";

import {
  ChatsCreateResponseMessagesItem,
  External$ChatsCreateResponseMessagesItem,
  Schemas$ChatsCreateResponseMessagesItem,
} from "./chats-create-response-messages-item";

/**
 * ChatsCreateResponse
 */
export type ChatsCreateResponse = {
  createdAt: string;
  id: string;
  messages: ChatsCreateResponseMessagesItem[];
  name: string;
  status: "ERROR" | "PROCESSED" | "PROCESSING";
  type: "AGENT/BUILD/WORKFLOW" | "DEEP_RESEARCH" | "RAG";
  userId: string;
};

/**
 * @internal
 * ChatsCreateResponse without any key transformation, this is what
 * we expect to come in as network data
 */
export type External$ChatsCreateResponse = {
  created_at: string;
  id: string;
  messages: External$ChatsCreateResponseMessagesItem[];
  name: string;
  status: "ERROR" | "PROCESSED" | "PROCESSING";
  type: "AGENT/BUILD/WORKFLOW" | "DEEP_RESEARCH" | "RAG";
  user_id: string;
};

/**
 * Takes network data, validates it, and transforms keys to match typescript object ChatsCreateResponse
 */
const SchemaIn$ChatsCreateResponse: z.ZodType<
  ChatsCreateResponse, // output type of this zod object
  z.ZodTypeDef,
  unknown
> = z
  .object({
    created_at: z.string(),
    id: z.string(),
    messages: z.array(Schemas$ChatsCreateResponseMessagesItem.in),
    name: z.string(),
    status: z.enum(["ERROR", "PROCESSED", "PROCESSING"]),
    type: z.enum(["AGENT/BUILD/WORKFLOW", "DEEP_RESEARCH", "RAG"]),
    user_id: z.string(),
  })
  .transform((obj) => {
    return zodTransform(obj, {
      created_at: "createdAt",
      id: "id",
      messages: "messages",
      name: "name",
      status: "status",
      type: "type",
      user_id: "userId",
    });
  });

/**
 * @internal
 * Takes typescript data, validates it, and maps keys to match the expected external object External$ChatsCreateResponse
 */
const SchemaOut$ChatsCreateResponse: z.ZodType<
  External$ChatsCreateResponse, // output type of this zod object
  z.ZodTypeDef,
  ChatsCreateResponse // the object to be transformed
> = z
  .object({
    createdAt: z.string(),
    id: z.string(),
    messages: z.array(Schemas$ChatsCreateResponseMessagesItem.out),
    name: z.string(),
    status: z.enum(["ERROR", "PROCESSED", "PROCESSING"]),
    type: z.enum(["AGENT/BUILD/WORKFLOW", "DEEP_RESEARCH", "RAG"]),
    userId: z.string(),
  })
  .transform((obj) => {
    return zodTransform(obj, {
      createdAt: "created_at",
      id: "id",
      messages: "messages",
      name: "name",
      status: "status",
      type: "type",
      userId: "user_id",
    });
  });

export const Schemas$ChatsCreateResponse = {
  in: SchemaIn$ChatsCreateResponse,
  out: SchemaOut$ChatsCreateResponse,
};
