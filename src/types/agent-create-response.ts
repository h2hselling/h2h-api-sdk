/**
 * Generated by Sideko (sideko.dev)
 **/

import { zodTransform } from "make-api-request-js";
import * as z from "zod";

import {
  AgentCreateResponseMessagesItem,
  External$AgentCreateResponseMessagesItem,
  Schemas$AgentCreateResponseMessagesItem,
} from "./agent-create-response-messages-item";

/**
 * AgentCreateResponse
 */
export type AgentCreateResponse = {
  /**
   * The agent that handled the request
   */
  agent: string;
  /**
   * The chat session ID (returned for convenience)
   */
  chatId?: string | undefined;
  /**
   * Display messages including user, assistant, and subcall (tool activity) messages
   */
  messages: AgentCreateResponseMessagesItem[];
  /**
   * The final response text from the agent
   */
  text: string;
};

/**
 * @internal
 * AgentCreateResponse without any key transformation, this is what
 * we expect to come in as network data
 */
export type External$AgentCreateResponse = {
  agent: string;
  chat_id?: string | undefined;
  messages: External$AgentCreateResponseMessagesItem[];
  text: string;
};

/**
 * Takes network data, validates it, and transforms keys to match typescript object AgentCreateResponse
 */
const SchemaIn$AgentCreateResponse: z.ZodType<
  AgentCreateResponse, // output type of this zod object
  z.ZodTypeDef,
  unknown
> = z
  .object({
    agent: z.string(),
    chat_id: z.string().optional(),
    messages: z.array(Schemas$AgentCreateResponseMessagesItem.in),
    text: z.string(),
  })
  .transform((obj) => {
    return zodTransform(obj, {
      agent: "agent",
      chat_id: "chatId",
      messages: "messages",
      text: "text",
    });
  });

/**
 * @internal
 * Takes typescript data, validates it, and maps keys to match the expected external object External$AgentCreateResponse
 */
const SchemaOut$AgentCreateResponse: z.ZodType<
  External$AgentCreateResponse, // output type of this zod object
  z.ZodTypeDef,
  AgentCreateResponse // the object to be transformed
> = z
  .object({
    agent: z.string(),
    chatId: z.string().optional(),
    messages: z.array(Schemas$AgentCreateResponseMessagesItem.out),
    text: z.string(),
  })
  .transform((obj) => {
    return zodTransform(obj, {
      agent: "agent",
      chatId: "chat_id",
      messages: "messages",
      text: "text",
    });
  });

export const Schemas$AgentCreateResponse = {
  in: SchemaIn$AgentCreateResponse,
  out: SchemaOut$AgentCreateResponse,
};
