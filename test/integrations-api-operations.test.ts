/**
 * Generated by Sideko (sideko.dev)
 **/

import { Client, Environment } from "api_ts";

describe("tests client.integrations.api.operations.delete", () => {
  test.concurrent(
    "DELETE /integrations/api/{id}/operations/{operationId} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.integrations.api.operations
          .delete({ id: "string", operationId: "string" })
          .asResponse(),
        client.integrations.api.operations.delete({
          id: "string",
          operationId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.integrations.api.operations.get", () => {
  test.concurrent(
    "GET /integrations/api/{id}/operations/{operationId} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.integrations.api.operations
          .get({ id: "string", operationId: "string" })
          .asResponse(),
        client.integrations.api.operations.get({
          id: "string",
          operationId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.integrations.api.operations.patch", () => {
  test.concurrent(
    "PATCH /integrations/api/{id}/operations/{operationId} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.integrations.api.operations
          .patch({
            data: { method: "DELETE", path: "string", payloadSchema: {} },
            id: "string",
            operationId: "string",
          })
          .asResponse(),
        client.integrations.api.operations.patch({
          data: { method: "DELETE", path: "string", payloadSchema: {} },
          id: "string",
          operationId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.integrations.api.operations.create", () => {
  test.concurrent(
    "POST /integrations/api/{id}/operations | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.integrations.api.operations
          .create({
            data: {
              method: "DELETE",
              name: "string",
              path: "string",
              payloadSchema: {},
            },
            id: "string",
          })
          .asResponse(),
        client.integrations.api.operations.create({
          data: {
            method: "DELETE",
            name: "string",
            path: "string",
            payloadSchema: {},
          },
          id: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
