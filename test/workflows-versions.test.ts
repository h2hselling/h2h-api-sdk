/**
 * Generated by Sideko (sideko.dev)
 **/

import { Client, Environment } from "api_ts";

describe("tests client.workflows.versions.delete", () => {
  test.concurrent(
    "DELETE /workflows/{origin_id}/versions/{version_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions
          .delete({ originId: "string", versionId: "string" })
          .asResponse(),
        client.workflows.versions.delete({
          originId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.list", () => {
  test.concurrent(
    "GET /workflows/{origin_id}/versions | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions
          .list({ originId: "string", limit: 123.45, page: 123.45 })
          .asResponse(),
        client.workflows.versions.list({
          originId: "string",
          limit: 123.45,
          page: 123.45,
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
  test.concurrent(
    "GET /workflows/{origin_id}/versions | testId: success_required_only | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions.list({ originId: "string" }).asResponse(),
        client.workflows.versions.list({ originId: "string" }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.get", () => {
  test.concurrent(
    "GET /workflows/{origin_id}/versions/{version_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions
          .get({ originId: "string", versionId: "string" })
          .asResponse(),
        client.workflows.versions.get({
          originId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.patch", () => {
  test.concurrent(
    "PATCH /workflows/{origin_id}/versions/{version_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions
          .patch({
            active: true,
            description: "string",
            name: "string",
            originId: "string",
            versionId: "string",
          })
          .asResponse(),
        client.workflows.versions.patch({
          active: true,
          description: "string",
          name: "string",
          originId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.create", () => {
  test.concurrent(
    "POST /workflows/{origin_id}/versions | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions
          .create({
            active: true,
            description: "string",
            name: "string",
            tasks: [
              {
                config: {},
                createdAt: "string",
                description: "string",
                id: "string",
                name: "string",
                outputSchema: {},
                parentTasks: {},
                toolId: "string",
                uiConfig: {},
                workflowId: "string",
              },
            ],
            originId: "string",
          })
          .asResponse(),
        client.workflows.versions.create({
          active: true,
          description: "string",
          name: "string",
          tasks: [
            {
              config: {},
              createdAt: "string",
              description: "string",
              id: "string",
              name: "string",
              outputSchema: {},
              parentTasks: {},
              toolId: "string",
              uiConfig: {},
              workflowId: "string",
            },
          ],
          originId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
