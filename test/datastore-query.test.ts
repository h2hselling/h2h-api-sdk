/**
 * Generated by Sideko (sideko.dev)
 **/

import { Client, Environment } from "api_ts";

describe("tests client.datastore.query.create", () => {
  test.concurrent(
    "POST /datastore/{id}/query | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.datastore.query
          .create({
            filters: {
              endDate: "1970-01-01T00:00:00",
              items: ["string"],
              similarityThreshold: 123.45,
              startDate: "1970-01-01T00:00:00",
            },
            limit: 123.45,
            query: "string",
            id: "string",
          })
          .asResponse(),
        client.datastore.query.create({
          filters: {
            endDate: "1970-01-01T00:00:00",
            items: ["string"],
            similarityThreshold: 123.45,
            startDate: "1970-01-01T00:00:00",
          },
          limit: 123.45,
          query: "string",
          id: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
