/**
 * Generated by Sideko (sideko.dev)
 **/

import { Client, Environment } from "api_ts";

describe("tests client.workflows.versions.tasks.delete", () => {
  test.concurrent(
    "DELETE /workflows/versions/{version_id}/tasks/{task_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions.tasks
          .delete({ taskId: "string", versionId: "string" })
          .asResponse(),
        client.workflows.versions.tasks.delete({
          taskId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.tasks.get", () => {
  test.concurrent(
    "GET /workflows/versions/{version_id}/tasks/{task_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions.tasks
          .get({ taskId: "string", versionId: "string" })
          .asResponse(),
        client.workflows.versions.tasks.get({
          taskId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.tasks.patch", () => {
  test.concurrent(
    "PATCH /workflows/versions/{version_id}/tasks/{task_id} | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions.tasks
          .patch({
            config: {},
            description: "string",
            id: "string",
            name: "string",
            outputSchema: {},
            parentTasks: {},
            toolName: "string",
            type: "delete",
            taskId: "string",
            versionId: "string",
          })
          .asResponse(),
        client.workflows.versions.tasks.patch({
          config: {},
          description: "string",
          id: "string",
          name: "string",
          outputSchema: {},
          parentTasks: {},
          toolName: "string",
          type: "delete",
          taskId: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});

describe("tests client.workflows.versions.tasks.create", () => {
  test.concurrent(
    "POST /workflows/versions/{version_id}/tasks | testId: success_all_params | Success test with response schema validation. Expects status code 200",
    async () => {
      const client = new Client({
        apiKey: "API_KEY",
        token: "API_TOKEN",
        environment: Environment.MockServer,
      });
      // Get both raw response for status and parsed response for data
      const [rawResponse, response] = await Promise.all([
        client.workflows.versions.tasks
          .create({
            config: {},
            description: "string",
            name: "string",
            outputSchema: {},
            parentTasks: {},
            toolName: "string",
            versionId: "string",
          })
          .asResponse(),
        client.workflows.versions.tasks.create({
          config: {},
          description: "string",
          name: "string",
          outputSchema: {},
          parentTasks: {},
          toolName: "string",
          versionId: "string",
        }),
      ]);
      expect(rawResponse.status).toBe(200); // Exact status code match
      // Response body automatically validated by Zod schema during deserialization
      expect(response).toBeDefined();
    },
  );
});
